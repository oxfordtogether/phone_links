<%= form_with(model: pod, local: true) do |form| %>
  <% if pod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pod.errors.count, "error") %> prohibited this pod from being saved:</h2>
      <ul>
        <% pod.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="field">
    <%= form.label :pod_leader %>
    <%= form.text_field :pod_leader %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
