<% content_for :page_head do %>
  <%= render PodHeadComponent.new(pod: @pod) %>
<% end %>
<% if @pod.matches.length > 0 %>
  <%= table @pod.matches.sort_by { |m| m.caller.name }, row_url: ->(m) { url_for(m) } do |tbl| %>
    <% tbl.col "Caller" do |match| %>
      <%= match.caller.name %>
    <% end %>
    <% tbl.col "Callee" do |match| %>
      <%= match.callee.name %>
    <% end %>
    <% tbl.col "Dates" do |match| %>
      <%= format_date_range(match) %>
    <% end %>
  <% end %>
<% else %>
  <div class="flex justify-center text-gray-600">
    No matches have been added to this pod yet :(
  </div>
<% end %>
